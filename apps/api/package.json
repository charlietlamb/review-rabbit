{
  "name": "api",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "build": "cd ../.. && docker build -t dubble-api -f apps/api/Dockerfile .",
    "start": "docker run --rm -p 8000:8000 dubble-api"
  },
  "dependencies": { "@neondatabase/serverless": "^0.10.3" },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
