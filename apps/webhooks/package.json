{
  "name": "@burse/webhooks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "bun esbuild.config.js",
    "deploy": "bun run build && wrangler deploy",
    "lint": "eslint src/",
    "clean": "rm -rf .turbo node_modules dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@burse/http": "workspace:*",
    "@burse/env": "workspace:*",
    "hono": "^3.11.7",
    "stripe": "^14.10.0",
    "esbuild": "^0.19.11"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@types/bun": "latest",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  }
}
